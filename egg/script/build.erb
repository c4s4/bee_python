# Build information
- build:       <%= name %>
  description: Build project <%= name.capitalize %>
  extends:     ":python:python.yml"

# Build properties
- properties:
    name:       <%= name %>
    py_src_dir: "."
